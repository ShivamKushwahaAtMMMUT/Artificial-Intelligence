#pragma once

typedef struct SAConstants
{
	const double MIN_COORDINATEX = -2;
	const double MAX_COORDINATEX = 2;
	const double MIN_TEMPERATURE = 1;
	const double MAX_TEMPERATURE = 100;
	const double COOLING_RATE = 0.02;
} SAConstants;